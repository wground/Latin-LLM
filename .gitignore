# IDE / editor
.vscode/
.claude/

# OS
.DS_Store

# Project-specific
NanoChat Reference/
src/Training Data/
out-latin/
latin_corpus_merged.txt
latin_training_config.json

# Rebuilt datasets (regenerate with: python3 src/prepare_corpus.py)
src/gpt_data_latin_v2/

# Corpus acquisition. Kept out of the repo by request. Note the tradeoff: without it,
# src/fetch_manifest.jsonl records WHAT was fetched and under which licence, but not how
# to reproduce the fetch.
src/fetch_corpus.py

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/

# Root-level build artifacts are no longer produced: paths.py anchors every artifact
# under src/ regardless of the directory a script is invoked from. Kept as a guard.
/gpt_data_latin/
/tokenizer_latin/
/out-latin/
